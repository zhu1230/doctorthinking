<%=render :partial => "invite_menu"%>

<table>
  <tr>
  </tr>

<% for invite in @invites %>
  <tr>
	<td><%=invite.email%></td>
    <td><%= link_to '显示详细内容', invite %></td>
    <td><%#= link_to '修改', edit_invite_path(invite) %></td>
    <td><%= link_to '删除', invite, :confirm => '你确定删除吗?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '发送新邀请', new_invite_path %>
